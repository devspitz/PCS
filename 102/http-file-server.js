const http = require('http');
const fs = require('fs');

//needs help!
const server = hhtp.createServercallback(request, response) { 
    let src = fs.createReadStream(process.argv[3], ()=>{
    }
 src.pipe(dst);
 }
server.listen(process.argv[2]);