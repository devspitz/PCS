export default function (){
return (

    <div class="container">
        {{ title }}
        {{ subtitle }}
        <nav>
            <NavLinks to="/">Home</NavLinks>
            <NavLinks to="/addPost">addPost</NavLinks>
        </nav>
        </div>
)}